package Screens.BruteForceProgress;
/*

import Agent.DM;
import Engine.EngineImpl;
import UI.UIAdapter;
import com.sun.org.apache.bcel.internal.generic.PUSH;
import javafx.concurrent.Task;

import java.util.Observable;

public class CollectDataTask extends Task<Boolean> {
private UIAdapter uiAdapter;
private int totalTask;
private DM dm;
private EngineImpl MainEngine;
private String level;
public CollectDataTask(EngineImpl MainEngine, UIAdapter uiAdapter, DM dm,String level){
    this.MainEngine=MainEngine;
    this.uiAdapter=uiAdapter;
    this.dm=dm;
    this.level=level;

}
    @Override
    protected Boolean call() throws Exception {
        MainEngine.startBruteForce(dm,level);

        return true;
    }

    public void update(int currentSize){

        try {
            Thread.sleep(30);
        } catch (InterruptedException e) {
            throw new RuntimeException(e);
        }


        updateProgress(currentSize,totalTask);
    }

    public void setTotalTask(int totalTask){
    this.totalTask=totalTask;
    }

}*/
